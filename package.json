{
  "name": "readaloud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "zip": "npm run build && cd dist && zip -r -FS ../readaloud.zip ."
  },
  "dependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.32",
    "@huggingface/transformers": "^3.7.2",
    "@mozilla/readability": "^0.5.0",
    "kokoro-js": "^1.2.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "@types/node": "^24.0.1",
    "vite-plugin-static-copy": "^3.4.0"
  }
}
